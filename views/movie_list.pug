extends layout

block content
  h1 Movies
  each movie in movies
    .movie-card
      h2 #{movie.name} (#{movie.year})
      p Rating: #{movie.rating}/10
      p Genres: #{movie.genres.join(', ')}
      a(href="/movies/" + movie._id) View Details